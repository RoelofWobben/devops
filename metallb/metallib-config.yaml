apiVersion: metallib.io/v1beta1
kind: IPAdressPool
metadata:
    name: default-pool
    namespace: metallb-system
spec:
    adresses:
        - 172.18.255.200-172.18.255.250

---

apiVersion: metallib.io/v1beta1
kind: L2Advertisement
metadata:
    name: default
    namespace: metallb-system

